<h1>編集しろ</h1>

<%= form_with model: @list, url: list_update_path(@list.id), method: :patch do |f| %>

<h4>タイトル</h4>
<%= f.text_field:title %>

<h4>本文</h4>
<%= f.text_area:body %>

<%= f.submit 'ここ！' %>

<% end %>
